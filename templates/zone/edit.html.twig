{% extends 'base.html.twig' %}

{% block title %}Modifier {{ zone.nom }}{% endblock %}

{% block main_content %}
    <h1 class="mb-4">Modifier {{ zone.nom }}</h1>

    {{ include('zone/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('zone_index') }}" class="btn btn-secondary">Retour à la liste</a>

    {{ include('zone/_delete_form.html.twig') }}
{% endblock %}
